/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DataTPipe } from './DataT.pipe';

describe('Pipe: DataTe', () => {
  it('create an instance', () => {
    let pipe = new DataTPipe();
    expect(pipe).toBeTruthy();
  });
});
